{
  "name": "your-app",
  "version": "1.0.0",
  "description": "Your application description",
  "main": "index.js",
  "scripts": {
    "compile": "tsc && node ./dist/index.js",
    "start:dev": "nodemon -e ts --exec \"npm run compile\"",
    "swagger-ui": "swagger-ui-express api.yaml",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "swagger-ui-express": "^5.0.1",
    "yamljs": "^0.3.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/swagger-ui-express": "^4.1.6",
    "@types/yamljs": "^0.2.34"
  }
}
